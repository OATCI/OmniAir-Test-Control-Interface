--
-- V2X Capture Metadata Profile
-- Japan ARIB STD-T760 / ITS-Connect RAT-specific metadata.
--
-- This branch is independent of IEEE 802.11p and cellular sidelink.
-- It permits a regional radio representation without overloading the
-- common observer model or another RAT's native packet
-- representation.
--

V2X-Capture-ARIB-T760
{ iso(1) identified-organization(3) v2xCapture(99999)
  aribT760(5) version1(1) }
DEFINITIONS AUTOMATIC TAGS ::= BEGIN

IMPORTS
    MeasurementValue, ExtensionContainers
FROM V2X-Capture-Common;

-- The base profile intentionally keeps the ARIB branch small. Exact
-- regional fields can be standardized later or placed in extensions.
AribT760Metadata ::= SEQUENCE {
    dataRate500Kbps        MeasurementValue OPTIONAL,
    txPowerDbmX10          MeasurementValue OPTIONAL,
    extensions             ExtensionContainers OPTIONAL,
    ...
}

END
